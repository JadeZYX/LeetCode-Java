public class P0374binarySearch_GuessNumber {
    public int guessNumber(int n){
        int left=1,right=n;
        while(left<=right){
            // int mid=left+(right-left)/2;
            // if(guess(mid)==0){//调用API，根据API返回的结果改变左右边界
            //     return mid;
            // }
            // if(guess(mid)==1){ //调用API来改变左右值
            //     left=mid+1;
            // }
            // else{
            //     right=mid-1;
            // }
        }
        return -1;//如果找不到返回-1
    }
}
